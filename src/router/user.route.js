import { Router } from "express";
import * as userRouter from "../controllers/user.controllers.js";

const routerUser = Router();

routerUser.post('/register', (req, res)=>{
    
});
routerUser.post('/login', (req, res)=>{
    
});

export default routerUser;